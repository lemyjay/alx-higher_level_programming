#!/usr/bin/node
// A script that prints 3 lines, but the lines would be in an array.
const lines = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

for (let i = 0; i < lines.length; i++) {
  console.log(lines[i]);
}
